/**
 * AeroVista Static Image Utility
 * A utility for static image handling with native lazy loading
 */

/**
 * Initialize image handling for static sites
 * This function does nothing now as we're using native lazy loading
 * with the loading="lazy" attribute directly in HTML
 */
function initLazyLoading() {
  // Native lazy loading is now handled by the browser
  // with the loading="lazy" attribute
  console.log('Using native lazy loading for images');
}

// Export the functions - keeping the same export signature
// for backward compatibility
export {
  initLazyLoading
}; 